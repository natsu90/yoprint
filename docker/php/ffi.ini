[PHP]
; FFI is always available under the CLI SAPI, but `artisan serve` handles
; requests with the cli-server SAPI, where ffi.enable defaults to "preload"
; and blocks FFI::cdef(). The DuckDB library needs it at request time.
ffi.enable = 1
